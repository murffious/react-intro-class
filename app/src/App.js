import React, { Component } from 'react';
import './App.css';
import Calculator from './Calculator'

class App extends Component {
  render() {
    return (
      <Calculator/>
    );
  }
}

export default App;

//  like prototype needs no function key word, destructuring - same name key prop, shortcut no need for key value pair if the same  , make sure not to wrap in curly braces when your not supposed to. 